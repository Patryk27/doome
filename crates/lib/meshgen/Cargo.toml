[package]
name = "doome-meshgen"
version = "0.1.0"
edition = "2021"

[dependencies]
# Workspace
doome-shader-common = { path = "../shader-common" }

# Crates.io
glam = { version = "0.22", default-features = false }

# Doomé

Doom + Cliché = Doomé; a game.

## Running locally

Requirements:

- rustup (Cargo & rustc),
- spirv-tools.

``` shell
$ git clone https://github.com/Patryk27/doome
$ cd doome
$ cargo run --release
# or:
$ cargo run-wasm
```
